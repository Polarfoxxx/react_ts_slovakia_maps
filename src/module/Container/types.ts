

export type TypeCitesObject = {
    meno: string,
    psc: string,
    obyvatelia: number,
    coordinates: {
        latitude: number,
        longitude: number
    }
}

export type TypeCitesArray = {
    meno: string,
    psc: string,
    obyvatelia: number,
    coordinates: {
        latitude: number,
        longitude: number
    }
}[]